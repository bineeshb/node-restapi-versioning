{
  "name": "node-restapi-versioning",
  "version": "1.0.0",
  "description": "Node.js REST API versioning",
  "main": "dist/app.js",
  "scripts": {
    "compile": "tsc && node dist/app.js",
    "dev": "nodemon -e ts --exec \"npm run compile\"",
    "start": "tsc -p tsconfig.prod.json && set NODE_ENV=production&& node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node.js",
    "rest",
    "api",
    "express",
    "typescript"
  ],
  "author": "Bineesh Balasubramanian",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/morgan": "^1.9.2",
    "nodemon": "^2.0.6",
    "tslint": "^6.1.3",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  }
}
